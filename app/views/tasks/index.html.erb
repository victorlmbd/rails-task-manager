<%= "My tasks" %>
<ul>
  <% @tasks.each do|task|%>
    <li><%=link_to task.title, task_path(task.id) %>| <%= link_to "Delete", task_path(task.id),  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
 %></li>

  <% end %>
</ul>

<%= link_to "add a new task", new_task_path %>
